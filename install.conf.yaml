- defaults:
    link:
      relink: true

- clean: ["~"]

- link:
    ~/.zshrc:
    ~/.gitconfig:
    ~/.ssh/config:

- create:
    - ~/OneDrive
    - ~/Nextcloud
- shell:
#     - [git submodule update --init --recursive, Installing submodules]
    - echo "hey you guys"
    - sleep 2
    -
        command: ./test.zsh
        stdout: true
        stderr: true
