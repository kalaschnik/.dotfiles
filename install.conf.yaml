- defaults:
    link:
      relink: true

- clean: ["~"]

- link:
    ~/.zshrc:
    ~/.gitconfig:

- create:
    - ~/OneDrive
    - ~/Nextcloud
# - shell:
#     - [git submodule update --init --recursive, Installing submodules]
