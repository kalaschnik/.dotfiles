- defaults:
    link:
      relink: true

- clean: ['~']
- link:
    ~/.Rprofile:
    ~/.zshrc:
    ~/.zprofile:
    ~/.gitconfig:
# - create: # if you create folder make sure they match the chmod the original app would create them with
#     - ~/OneDrive
#     - ~/Nextcloud
# - shell:
#     -
#         command: ./setup_homebrew.zsh
#         stdout: true
#         stderr: true
